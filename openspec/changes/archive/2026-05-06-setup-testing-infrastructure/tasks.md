## 1. Infrastructure

- [x] 1.1 Install devDependencies: `vitest`, `@vitejs/plugin-react`, `jsdom`, `@testing-library/react`, `@testing-library/dom`.

## 2. Configuration

- [x] 2.1 Create `vitest.config.ts` in the root directory.
- [x] 2.2 Update `package.json` with scripts: `"test": "vitest run"`, `"test:watch": "vitest"`.

## 3. Verification

- [x] 3.1 Create a sample test file `src/utils/math.test.ts` to verify the setup.
- [x] 3.2 Run `npm run test` and ensure the sample test passes.
